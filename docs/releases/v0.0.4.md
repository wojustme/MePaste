## MePaste v0.0.4

本次更新修复历史面板关闭后的焦点恢复体验，让复制历史内容后可以更自然地回到原来的应用继续输入。

### 修复

- 修复通过快捷键打开历史面板并选择记录后，焦点可能停留在 MePaste、没有回到原前台应用的问题。
- 修复按 Esc 关闭历史面板时，焦点恢复行为不够一致的问题。

### 改进

- 历史面板打开时会记录之前的前台应用，面板主动关闭或复制记录后会尝试把焦点交还给该应用。
- 如果用户是点击到其他应用导致面板失焦关闭，则尊重 macOS 当前焦点，不额外抢回之前记录的应用。
