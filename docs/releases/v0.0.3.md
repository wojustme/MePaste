## MePaste v0.0.3

本次更新修复历史面板搜索状态下的键盘导航体验，让搜索、中文输入和方向键选择可以自然地配合使用。

### 修复

- 修复搜索框获得焦点后，左右方向键只移动文本光标、无法切换历史记录选中项的问题。
- 保留中文输入法组词状态下的原生按键行为，避免候选词选择或合成文本被历史面板快捷键误拦截。

### 改进

- 将历史面板键盘处理中的回车、Esc、左右方向键改为 Carbon 键码常量，降低后续维护时误改键位的风险。
